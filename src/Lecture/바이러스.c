#include <stdio.h>
#define N 110
#define K 1010
int data[N][K];
int n, k, m;

void ffun()
{
	for (int i = 1; i <= n; i++)
	{
		if (i - 1 == temp)
		{

		}
	}
}

int main()
{
	FILE *f;
	freopen_s(&f, "input.txt", "r", stdin);
	freopen_s(&f, "output.txt", "w", stdout);

	scanf_s("%d %d", &n, &k);

	for (int i = 1; i <= n; i++)
	{
		scanf_s("%d", &m);

	}
	return 0;
}