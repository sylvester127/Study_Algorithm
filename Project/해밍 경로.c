#include <stdio.h>
#define N 100010
//n이 10만, 비트수 30
int n, k;
int main()
{
	FILE *f;
	freopen_s(&f, "input.txt", "r", stdin);
	freopen_s(&f, "output.txt", "w", stdout);

	scanf_s("%d %d", &n, &n);

	for (int i = 1; i <= n; i++)
	{
		scanf_s("%d", &data);
	}
	return 0;
}