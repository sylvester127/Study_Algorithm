#include <stdio.h>
#include <stdlib.h>
int main()
{
	FILE *f;
	freopen_s(&f, "input.txt", "r", stdin);
	freopen_s(&f, "output.txt", "w", stdout);

	int num, i;

	scanf_s("%d", &num);
	malloc(int(size))
	gets_s(arr, sizeof(arr));
	return 0;
}